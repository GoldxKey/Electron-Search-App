import React, { Component, PropTypes } from 'react';

class OpenConfig extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div className="setting__open_config">

      </div>
    )
  }
}

export default OpenConfig;
